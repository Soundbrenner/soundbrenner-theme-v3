<section class="sb-body-text" data-sb-reveal>
  <div class="sb-body-text__content font-body weight-regular rte" data-sb-reveal-item>
    {{ section.settings.content }}
  </div>
</section>

{% stylesheet %}
  .sb-body-text__content {
    color: var(--sb-color-neutral-900);
  }

  .sb-body-text__content > * {
    margin: 0;
  }

  .sb-body-text__content > * + * {
    margin-top: var(--sb-space-24);
  }
{% endstylesheet %}

{% schema %}
{
  "name": "Body text",
  "settings": [
    {
      "type": "richtext",
      "id": "content",
      "label": "Content",
      "default": "<p>Add your student discount details here.</p>"
    }
  ],
  "presets": [
    {
      "name": "Body text"
    }
  ]
}
{% endschema %}
